
Sudo code for format

loop:
  If cell_key pressed:
    fifo.add(cell_key)
  If DAC_key pressed:
    writeDAC(DAC_key) // probably have a value be typed after?
  else ignore keypress

  sendShock(cell[cell_key]->cell_id)
